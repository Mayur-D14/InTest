# Copy this file to .env in the same directory as docker-compose.yml
# docker compose automatically loads .env — no extra flags needed.

# GitHub Personal Access Token with 'repo' and 'actions:read' scope.
# Required only if you're using the CI/CD Pipelines feature (Phase 4).
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
